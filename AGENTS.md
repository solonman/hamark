# 视频创意脚本系统｜Codex 工程授权边界

本文件记录项目负责人对 Codex 在本仓库内持续推进工程任务的长期授权。它减少不必要的人工确认，但不覆盖 Codex 平台自身的沙箱、安全审查和审批规则。

## 可直接执行

在任务目标已经明确的前提下，以下操作无需再次向项目负责人确认：

- 阅读、搜索、创建和修改本仓库内的代码、测试、文档、样式、迁移和配置；
- 运行构建、测试、类型检查、静态检查、格式化、只读诊断和本地浏览器验收；
- 启动、停止或重启本项目的本地开发服务；
- 执行非破坏性的本地数据库建表、增量迁移和演示数据验证；
- 在当前仓库内查看差异、创建分支、暂存、提交和推送已经完成并验证的任务；
- 修正实现过程中发现的、与当前任务直接相关且不改变已冻结产品含义的问题。

## 必须暂停并请求批准

遇到下列任一情况，必须说明具体对象、用途和风险，等待项目负责人批准：

1. 读取、传输、复制或暴露隐私数据、凭据、密钥、客户资料或其他敏感信息；
2. 删除本地文档、用户素材、历史作业、批准版本、业务数据，或执行难以恢复的覆盖／回滚；
3. 产生付款、订阅、付费 API 调用或其他实际费用；
4. 从外部下载或安装软件、依赖、二进制文件，或执行刚下载的内容；此时先说明来源、必要性和可验证的安全信息；
5. 对生产环境执行不可逆的数据变更、权限放宽或安全策略弱化。

## 执行原则

- 优先使用仓库已经锁定的依赖，不为方便而新增下载。
- 保留用户已有改动和全部历史快照，不以“清理”为由删除业务资料。
- 临时构建产物和由本次任务明确生成的临时测试数据，可以在确认目标后按项目惯例清理；不把用户源文件视为临时产物。
- 发现冻结说明与现有实现冲突时，以版本号更高的冻结说明为准；如果会改变产品含义，先停止并请求判断。
- 平台自动安全审查仍可阻止或升级高风险操作；不得规避系统审批。

## 项目任务分工

- 方法论／产品讨论任务负责理解需求、比较方案、冻结术语与交互、形成工程说明和验收标准；除非用户明确要求当前任务直接开发，否则不要在讨论未冻结时零散修改代码。
- 已有“代码工程”任务时，冻结后的功能开发、测试、Git 提交、GitHub 推送和部署核验统一交给该任务；讨论任务负责发送完整交接包、跟踪结果和做产品验收，不与代码工程并行重复实现同一功能。
- 不把产品决策留在临时对话里。长期有效的结论写入 `交付包/AI视频创意逆向工程_项目总纲与工作记忆_V0.1_20260812.md`；可执行约束写入本文件；专题细节写入版本化工程说明。
- 如果没有合适的代码工程任务，当前任务可以自行完成整条链路，但仍须遵循下面的推送、部署和数据变更规则。

## 标准 GitHub 推送与部署流程

所有需要进入共享网站的工程改动，默认采用“代码实现 → GitHub → 部署 → 线上验收”，不得把本机修改或本机数据库结果表述为线上完成。

1. **冻结输入**：开发前明确批准文件、变更范围、不可修改项和验收标准；发现含义冲突先回到产品任务确认。
2. **同步与隔离**：读取本文件和项目记忆；执行 `git status`、`git fetch`，核对 `origin/main` 与当前提交；保留用户未跟踪文件和其他任务改动，只暂存本任务文件。
3. **实现与本机验证**：代码、测试、迁移、文档一起完成；按风险运行 `npm test`、`npm run lint`、`npm run build` 和必要的本机浏览器验收。测试写入只允许使用隔离的 `TEST_ONLY` 数据并清理。
4. **提交前复核**：检查 `git diff --check`、暂存清单和测试结果；提交信息说明实际结果，不把“准备执行”写成“已经上线”。
5. **并发同步**：推送前再次 `git fetch`。如果远端有新提交，安全重放或快进整合，重新运行受影响测试；不得用强制推送覆盖其他任务。
6. **推送 GitHub**：推送经过验证的提交到约定分支；当前项目未另行指定时使用 `main`。
7. **部署核验**：GitHub 推送不等于部署完成。必须确认 Vercel／实际托管平台已经部署目标提交，可通过部署状态、提交 SHA、`/api/version` 或等价证据核对。
8. **线上验收**：在真实域名用授权身份验证关键用户流程，并区分只读检查与会写数据的动作；未经授权不得用真实业务案例做写入型冒烟测试。
9. **完整回报**：至少报告变更文件、测试结果、提交 SHA、GitHub 推送状态、线上部署 SHA／状态、线上验收结果和任何仍需人工完成的动作。

## 生产数据变更的代码化原则

- GitHub 推送只会发布程序，不会自动修改既有业务数据。凡涉及线上历史数据修复、映射、回填或迁移，默认实现为版本化、可测试、可审计的受控迁移程序，再经 GitHub 和部署链进入生产；不要把生产数据库凭据拉到本机后直接手工改库。
- 禁止在应用构建、普通部署或服务启动时自动执行一次性业务数据迁移；部署与数据 APPLY 必须分离。
- 受控迁移至少具备：固定作用域、管理员身份与同源校验、只读 PREVIEW／DRY-RUN、显式确认、事务与行锁、执行前可恢复备份、操作账本／审计日志、幂等防重放、写后不变量校验和异常回滚。
- 标准顺序为：部署迁移代码 → 线上只读 PREVIEW → 核对目标、数量、版本和不变量 → 在既有批准范围内 APPLY → 数据层复核 → 页面验收 → 将入口永久锁定或移除。
- PREVIEW 与批准方案一致时，已有明确执行批准覆盖正常实施步骤，不重复索要形式化批准；若线上版本、数量、作者、状态、活动标准版或作用范围与批准方案不一致，立即停止并请项目负责人重新判断。
- 不得因“只处理一个案例”而降低安全要求；不得修改其他案例、不可变快照、批准版本、审核历史、批注或修订事件，除非批准文件明确将其列为目标。
